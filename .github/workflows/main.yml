name: hello world workflow
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  hello:
    runs-on: ubuntu_latest
    steps:
      - uses: actions/checkout@v2
      - name: hello world
        run: echo "hello world tony"
        shell: bash
  goodbye:
    runs-on: ubuntu_latest
    steps:
      - name: goodbye
      - run: echo "goodbye"
        shell: bash